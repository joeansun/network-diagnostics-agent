# config.py is used for loading configurations from config.toml

import subprocess

# subprocess.run(
#     ["route", "-n", "get", "default"],
#     capture_output=True,
#     text=True,
#     check=False,
# )

def load_config():
    ...